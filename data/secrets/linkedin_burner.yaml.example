# Ops-only — NEVER commit. Product burner LinkedIn + Gmail for Layer 5 bootstrap.
# CLI: warm-bridge burner-login

email: burner@example.com
password: "CHANGE_ME"

# Prefer email OTP on burner — Gmail API reads verification mail:
gmail_credentials_json: data/secrets/gmail_oauth_client.json
gmail_token_json: data/secrets/gmail_token.json

# If burner uses authenticator app instead of email OTP:
# totp_secret: "BASE32SECRET"

# Optional separate profile for ops burner (defaults to session profile_dir):
# profile_dir: data/camoufox_profile_ops
