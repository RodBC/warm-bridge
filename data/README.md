# Private data

Copy examples, then edit:

```bash
cp profile/example.seller.yaml data/seller.yaml
cp profile/example.network.yaml data/network.yaml
```

These files are gitignored. Keep real contacts off GitHub.
