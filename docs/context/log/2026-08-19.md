## 09:30 — Stabilize, then outcome memory

- Done: Committed send-ready + account workspace as `4c4f8f9`; ignored `*.tsbuildinfo`; CLI `find --seller`; `docs/CSV_VALIDATION.md`; `outcomes.py` + `web/src/outcomes.ts` + ask-dock chips + Histórico; `POST /api/outcomes` validate/echo; eval `outcome_statuses_allowed`
- Decisions: Outcomes browser-first (no server PII); one feat commit for entangled tutor/account layers; outcomes before LLM/radar/spider-web
- Next: Real CSV validation (human) or choose P2 LLM vs landing

## 09:45 — LinkedIn-first UX (no scrape)

- Done: Shorter ask templates; WA phone normalize + URL clip; LinkedIn primary send; UI LinkedIn-first fields; `linkedin.py`; demo profile URLs; GH research logged (lean-intros/backchannel OK pattern; Selenium/Starnus = red)
- Decisions: Paradigm = LinkedIn as channel + identity; graph still demo/export — never auto-scrape in public core
- Next: User retest Copiar / LinkedIn / WhatsApp on Ana

## 09:55 — Lead Police spider board UI

- Done: Brand Lead Police; cork investigation board + red yarn edges; `SpiderBoard.tsx` + `avatar.ts` (SVG initials); person pins with company/title; dossier list + ask dock kept actionable; styles/fonts overhaul; AI_BUILD_MAP + CURRENT updated
- Decisions: Visual identity = police evidence board; no LinkedIn photo scrape (deterministic avatars); search improvements stay in parallel chat
- Next: Visual QA on demo find; optional account-mode multi-target board

## 10:05 — Default case Rodrigo → Sabrina

- Done: Seller/LinkedIn defaults; `example.lead-police.yaml` + API example-network; auto-find on load; portraits in `web/public/portraits/`; pin layout spacing + readable text; session v3; evals still on Marina network (11 pass)
- Decisions: Bridge cast is fictional demo (not real mutuals); Sabrina title/company from public presence; no LinkedIn photo scrape
- Next: Hard-refresh UI; optional real portrait files from user

## 10:30 — Demo opt-in + LinkedIn photos

- Done: Start vazio até **Ver demo**; removi cartoon `/portraits` do resolver; enrich default puxa `avatar_url` do LinkedIn (CDN) no map real; `referrerPolicy=no-referrer`; session v5
- Decisions: Foto = sessão LinkedIn ou iniciais; demo não inventa faces
- Next: Smoke test Chrome profile + Mapear via LinkedIn com fotos reais

## 10:40 — Photos, formal asks, draggable scatter board

- Done: Real jpg portraits (Rodrigo/Sabrina via unavatar + pravatar cast); avatar.ts no longer blocks /portraits; seller = Associate Engineer @ IQVIA; notes without Demo:; formal approach templates; scatter layout + drag pins + same-company cluster yarns; 9 bridges in lead-police graph
- Decisions: Photos for demo are cached under web/public/portraits; Selenium scrape exists but needs Chrome user-data-dir — not auto on /api/find
- Next: User hard-refresh + Ver demo; optional wire live avatar_url from selenium enrich into pins

## 10:50 — Board pin perfection (plan)

- Done: enrich_cap default 16; seller/target snapshot (title/company/headline/avatar) in linkedin-map meta + seller identity; UI `resolvePortrait` CDN-first (no /portraits happy path); `boardLinkedInHref` → null for fakes (no search); Mapear caso Sabrina replaces fictional Ver demo; session v7; `scripts/validate_board_pins.py`; `avatar.contract.test.ts`; docs CURRENT/AI_BUILD_MAP/LINKEDIN_SELENIUM; lead-police YAML marked offline-only
- Decisions: Canonical board = POST /api/linkedin-map + enrich; never invent cargo/photos; LI button only real /in/
- Blocked: Live Rodrigo→Sabrina smoke — no Linux Chrome in WSL (sudo blocked); session yaml points at data/chrome_profile/
- Next: Install google-chrome-stable + logged-in chrome_profile → validate_board_pins --live → browser QA

## 11:00 — Demo offline + npm run dev

- Done: Restored **Ver uma demo!** (example network + find, no Selenium); `scripts/setup.sh` + `scripts/dev.sh` + root `npm run dev` (API+UI); API via `.venv-wb` with `.[linkedin]`
- Decisions: Demo = offline fixture; Mapear via LinkedIn = live session
- Next: User hard-refresh → Ver uma demo!; live Chrome still optional

## 11:50 — Handoff finish (run + visuals + docs)

- Done: API healthy on `:8788` via `.venv-wb`; demo find → 8 bridges; hardened `scripts/dev.sh` (busy-port refuse + “API healthy”); visual craft (Career Fit atmosphere/frosted panels/motion + Lead Police cork noise, larger brand, mono bands); fixed CURRENT “Do not regress” demo contradiction; eval 12/12 + avatar contract OK
- Decisions: Steal Career Fit craft, keep cork identity; never `source .venv` for LinkedIn; demo fixture OK when labeled
- Blocked: Live Mapear — no Linux Chrome / no `data/chrome_profile/` (needs host sudo)
- Next: Install `google-chrome-stable` + profile → `validate_board_pins.py --live`

## 15:55 — Demo real LinkedIn + MUI (RWD craft)

- Done: `example.lead-police.yaml` with real 3S Checkout / E-Deploy `/in/` + unavatar; avatar.ts unavatar for any real slug; UI rewrite MUI 9 + Framer Motion (RWD navy/blue); session v8; board blue yarn chrome; demo alert labeled offline
- Decisions: Demo may use real public profiles (labeled); UI craft follows RWD Connect / rwd-website tokens via MUI
- Blocked: Live Chrome still missing in WSL
- Next: Hard-refresh → Ver uma demo! → confirm LinkedIn on every pin

## 16:50 — Next wedge: session gate + Casos

- Done: `GET /api/linkedin-session/status` + `session_status.py` / friendly Mapear errors; Briefing **Sessão LinkedIn** panel; `scripts/setup_chrome_profile.sh` + yaml.example; **Casos recentes** (`web/src/cases.ts`) + favor-bank cooldown in tutor/ask dock; PRODUCT/ARCHITECTURE/product skill aligned Selenium-primary; CURRENT + AI_BUILD_MAP updated
- Decisions: Live Chrome readiness is a product surface; Casos/favor-bank before radar/landing/LLM; docs no longer fight AGENTS
- Blocked: Live Mapear — still needs `google-chrome-stable` + LinkedIn login under `data/chrome_profile/`
- Next: Owner install Chrome → setup script login → `validate_board_pins.py --live` → UI Mapear Rodrigo→Sabrina

## 17:15 — Insight hybrid wedge (no LI login)

- Done: `research/` + `/api/research` + `/api/investigate`; approach hooks; UI import-first + Investigar; sources/AGENTS/PRODUCT/ARCHITECTURE/skills; eval `research_pack_structure` (13 pass)
- Decisions: Owned graph + public research primary; Selenium/demo demoted; never invent people from search
- Next: User exports Connections.csv → Investigar real target
