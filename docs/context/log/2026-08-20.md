
## 2026-08-20 — Camoufox session + Layer 5 burner OTP

- Done: `warm_bridge.linkedin_session` — Camoufox driver, Playwright mutuals/enrich, `session_status` (backend/burner hints), ops CLI `burner-login` / `gmail-auth` / `session-login`
- Done: Gmail OTP poller + `otp_inbox/parse`; pyotp for authenticator burner; `linkedin_selenium` re-export alias
- Done: `docs/LINKEDIN_SESSION.md`, sources.yaml rows, UI session types, eval `otp_parse_linkedin_email` + `session_status_structure`
- Decisions: LinkedIn scrape primary; CSV legacy; auto session from linkedin_account.yaml

## 2026-08-20 (pm) — LinkedIn-first product pivot

- Done: Auto session ensure on serve/map; UI Mapear primary; CSV demoted; sources.yaml + AGENTS primary flip
- Credentials path: data/secrets/linkedin_account.yaml

## 11:05 — Port Career Fit OTP / login state machine

- Done: `burner/bootstrap.py` — Career Fit state machine (`_await_post_password`, PT strings, OTP retry, feed nudge); `launch_persistent` in camoufox; IMAP year-filter + AUTH errors; App Password preflight; URL `_authed_url` probe; `logged_in_hint` in session_status
- Done: evals `challenge_classifier` + `app_password_preflight`; docs LINKEDIN_SESSION / CURRENT
- Decisions: SMS still out of scope; IMAP App Password stays primary (not Gmail OAuth)
- Next: paste real 16-char App Password → burner-login → dogfood Mapear

## 11:30 — Dogfood warm + context commit

- Done: Saved Gmail App Password; session already warm (`/login` → `/feed`); bootstrap short-circuit for existing profile; `scripts/dev.sh` on :8788/:5174
- Done: Updated account password in secrets (keep App Password); README LinkedIn-first; skills product/sources; `.gitignore` adds `.venv-linkedin/` + `tmp/`
- Done: Structured CURRENT session arc; commit + push (secrets never committed)
- Next: Mapear real target URL in UI
